<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.krashlandocodingtest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KrashlandoCodingTest">
        <activity
            android:name=".ShowDetail"
            android:label="@string/title_activity_show_detail"
            android:theme="@style/Theme.KrashlandoCodingTest.NoActionBar"></activity>
        <activity
            android:name=".ShowListActivity"
            android:label="@string/title_activity_show_list"
            android:theme="@style/Theme.KrashlandoCodingTest.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>